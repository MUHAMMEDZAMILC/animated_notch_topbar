name: animated_notch_topbar
description: >
  A customizable animated top app bar with a sliding notch-style tab
  indicator, per-tab gradient themes, and a built-in search field.
version: 0.0.1
homepage: https://github.com/your-username/animated_notch_topbar
repository: https://github.com/your-username/animated_notch_topbar
issue_tracker: https://github.com/your-username/animated_notch_topbar/issues

topics:
  - widget
  - tabbar
  - navigation
  - animation

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
